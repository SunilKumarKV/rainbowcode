# Initial GitHub Issues

## Issue 1: Initialize RainbowCode monorepo foundation
Create pnpm workspace, Next.js app, packages, config, CI, security docs.

## Issue 2: Build dashboard shell
Create authenticated dashboard layout with sidebar, topbar, project cards.

## Issue 3: Build editor shell
Create left sidebar, canvas, right properties panel, bottom generated code panel.

## Issue 4: Implement theme token engine
Create token schema, default theme, token preview, export format.

## Issue 5: Build button builder prototype
Allow visual button customization and generate React + Tailwind output.

## Issue 6: Add RBC CLI package skeleton
Create CLI package with `rbc init`, `rbc add`, `rbc theme` command plan.

## Issue 7: Add security and QA gates
Add CodeQL, Dependabot, lint, test, typecheck, build, Lighthouse CI plan.
